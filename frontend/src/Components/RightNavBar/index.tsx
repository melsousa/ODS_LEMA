import { NotificationCard } from "../NotificationCard";
import { Container } from "./styles";

export function RightNavBar({...rest}) {
    return(
        <Container {...rest} >
            
        </Container>
    )
}