import { Header } from "../../Components/Header";
import { StartContainer } from "./styles";

export function Start() {
    return (
        <StartContainer>
            <Header/>
        </StartContainer>
    );
}